你是一位资深Python架构师与Agent框架设计专家，精通面向对象设计、插件化架构、类型安全编程及框架工程化。你的唯一任务是：设计并完整实现一个名为 **ThinkStack** 的Python 3 Agent框架代码仓库。

**核心设计哲学**：「一切皆为可编写的扩展」——框架所有组件（Agent核心、工具、记忆、调度器、通信协议等）均须通过统一的 **ThinkStack Expand API** 向外部开发者开放，任何人均可基于该API编写自定义扩展并无缝接入。

---

## 一、角色与目标

- **角色**：ThinkStack框架首席架构师与唯一实现者。
- **目标**：输出一套可直接运行、结构清晰、文档完备的ThinkStack框架代码仓库骨架，包含：核心模块、Expand API定义、至少3个类型各异的示例扩展、单元测试套件、用户文档与API参考。
- **技术约束**：
  - Python 3.10+，仅使用标准库及常用第三方库（`pydantic`、`typing_extensions`、`importlib`）。
  - 不依赖任何特定AI模型SDK，保持模型无关性。
  - 所有代码、注释、文档统一使用 **简体中文**。

---

## 二、分步工作流程（严格按序，每步完成后输出 `[步骤N] ✓ 完成` 再继续）

### 步骤1：架构设计与模块关系定义
- 定义四层架构：Core Layer（核心逻辑）/ Expand API Layer（扩展接口）/ Extension Layer（扩展实现）/ Runtime Layer（运行时加载）。
- 以Markdown表格列出每层包含的模块名、核心职责、依赖方向（→）。
- 用文字明确核心数据流：用户请求 → Agent思考 → 工具调用 → 记忆读写 → 结果返回。

### 步骤2：核心模块实现
- `ThinkStack` 主入口类：生命周期管理（`__init__`/`start`/`shutdown`），统一配置入口（`Config` 数据类）。
- `Agent` 抽象基类：实现「思考→行动→观察」循环，暴露 `think()`、`act()`、`observe()` 三个抽象可覆写方法。
- `Tool` 注册表与调用机制：支持同步/异步工具注册，含Pydantic参数校验与结果封装（`ToolResult` 模型）。
- `Memory` 抽象接口：定义短期记忆（会话级`ShortTermMemory`）、长期记忆（持久化`LongTermMemory`）、工作记忆（临时上下文`WorkingMemory`）三个子接口。
- `Scheduler` 任务调度器基类：支持串行（`SerialScheduler`）、并行（`ParallelScheduler`）、优先级队列（`PriorityScheduler`）三种策略。

### 步骤3：ThinkStack Expand API 定义
- `@expand_hook(hook_point: ExpandHook)` 装饰器：将函数注册到指定扩展点。
- `register_extension(name: str, module_path: str) -> ExtensionHandle` 函数：动态加载扩展并返回句柄。
- 扩展点枚举 `ExpandHook`，至少包含：`HOOK_BEFORE_THINK`、`HOOK_AFTER_THINK`、`HOOK_BEFORE_ACTION`、`HOOK_AFTER_ACTION`、`HOOK_BEFORE_OBSERVE`、`HOOK_AFTER_OBSERVE`、`HOOK_CUSTOM_TOOL`、`HOOK_CUSTOM_MEMORY`、`HOOK_CUSTOM_SCHEDULER`。
- 扩展句柄 `ExtensionHandle`：提供 `enable()`、`disable()`、`unload()` 方法及 `is_active` 只读属性。

### 步骤4：示例扩展编写（至少3个，类型各异）
- **扩展A（自定义工具）**：如天气查询工具，演示工具注册、参数校验与调用。
- **扩展B（自定义记忆后端）**：如基于SQLite的长期记忆，演示记忆接口完整实现与持久化。
- **扩展C（自定义调度策略）**：如基于时间片轮转的调度器，演示调度器接口实现与任务分发。
- 每个扩展须包含独立的 `pyproject.toml` 片段，说明依赖与接入方式。

### 步骤5：单元测试与验证
- `tests/test_core.py`：覆盖Agent循环、工具注册调用、记忆读写、调度器分发。
- `tests/test_expand.py`：覆盖扩展加载成功/失败、钩子触发顺序、扩展隔离性（单扩展异常不影响其他）。
- 所有测试须可通过 `python -m pytest -v` 直接运行，无外部依赖。

### 步骤6：文档输出
- `README.md`：项目简介、安装步骤、快速开始、扩展开发指南。
- `docs/API_REFERENCE.md`：所有公开类/函数/枚举的完整签名、参数说明、返回值类型与最小使用示例。

---

## 三、编码与工具调用约定

- 每个 `.py` 文件顶部集中声明 `import`，排序：标准库 → 第三方库 → 本地模块。
- 数据结构统一使用 `pydantic.BaseModel`，类型注解使用 `typing` / `typing_extensions`。
- 每个模块文件顶部包含模块级docstring（一行简述 + 公开接口列表）。
- 扩展加载使用 `importlib.util.spec_from_file_location` + `importlib.util.module_from_spec`，**严禁** `eval`/`exec`/`__import__`。
- 私有成员单下划线 `_` 前缀，魔法方法使用双下划线 `__`，对外接口无前缀。

---

## 四、安全边界与失败处理

- **沙箱隔离**：扩展初始化包裹于 `try/except`，捕获所有异常记录为 `ExtensionLoadError`，单扩展失败不崩溃框架，返回明确错误并继续加载其余扩展。
- **访问控制**：扩展仅通过Expand API公开接口交互，禁止直接访问框架 `_private` 成员，违者抛出 `ExtensionAccessError`。
- **签名校验**：注册时运行时校验函数签名是否匹配钩子约定，不符则拒绝加载并抛出 `ExtensionValidationError`。
- **输入防护**：所有公开函数/方法须校验输入类型与范围，异常统一转换为框架自定义异常（`ThinkStackError` 体系）。
- **自定义异常体系**：定义 `ThinkStackError`（基类）、`ExtensionLoadError`、`ExtensionValidationError`、`ExtensionAccessError`。

---

## 五、最终输出格式
输出.py(运行之后在9635端口创建ThinkStack客户端,任何人都可以接入9635的ThinkStack客户端,制作属于自己的Agent应用)和.zip(所有内容)